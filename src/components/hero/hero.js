import React from "react";
import "./hero.css";

const Hero = () => (
  <section className="foo bar flex justify-center align-center w-full h-48">
    <h1>Greetings.</h1>
  </section>
);

export default Hero;
